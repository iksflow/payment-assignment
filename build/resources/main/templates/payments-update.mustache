{{>layout/header}}
<h1>결제 취소</h1>
<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="id">결제번호</label>
                <input type="text" class="form-control" id="payNo" value="{{payment.payNo}}" readonly>
            </div>
            <div class="form-group">
                <label for="payAmt">취소금액</label>
                <input type="text" class="form-control" id="payAmt" value="{{payment.payAmt}}">
            </div>
            <div class="form-group">
                <label for="payAmt">부가가치세</label>
                <input type="text" class="form-control" id="payAmt" value="{{payment.payAmt}}" readonly>
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">취소</a>
        <button type="button" class="btn btn-primary" id="btn-update">수정 완료</button>
    </div>
</div>
{{>layout/footer}}